import { ComentarioSituacion } from './comentario-situacion';

describe('ComentarioSituacion', () => {
  it('should create an instance', () => {
    expect(new ComentarioSituacion()).toBeTruthy();
  });
});
